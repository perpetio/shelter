import 'package:flutter/material.dart';

class ShLoadingScreen extends StatelessWidget {
  const ShLoadingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}

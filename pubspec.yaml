name: shelter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  easy_localization: ^3.0.0
  equatable: ^2.0.3
  flutter_bloc: ^8.0.1
  flutter_map: ^0.14.0
  flutter_map_marker_cluster: ^0.4.4
  flutter_screenutil: ^5.5.1
  font_awesome_flutter: ^10.1.0
  google_fonts: ^2.3.2
  latlong2: ^0.8.1
  persistent_bottom_nav_bar: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/translations/
    - assets/evacuation_lviv_region.json
